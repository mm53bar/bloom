<% content_for :title, "Editing #{@location.name}" %>
<h1 class="mb-6 text-2xl font-semibold tracking-tight text-stone-900">Editing <%= @location.name %></h1>
<%= render "form", location: @location %>
